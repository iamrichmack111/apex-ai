# 📚 Knowledge and RAG

Apex supports a local knowledge layer containing up to one million Q&A records.

Retrieval can combine:

- SQLite FTS
- lexical search
- semantic reranking
- embedding models
- conversation context
- long-term memory

Only relevant records are inserted into an AI prompt.
